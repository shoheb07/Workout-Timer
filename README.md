# Workout-Timer
The Workout Timer is a simple desktop application built using Python and Tkinter. It is designed to help users manage structured workout sessions by alternating between work intervals and rest intervals for a defined number of rounds
🏋️ Workout Timer (Python Tkinter)

📌 Overview

Workout Timer is a simple GUI-based fitness timer built using Python and Tkinter. It helps users perform interval training by automatically switching between workout and rest sessions.

🚀 Features

- Countdown timer display
- Work and Rest intervals
- Multiple rounds support
- Automatic session switching
- Completion popup message
- Simple and user-friendly interface


🛠️ Technologies Used

- Python 3.x
- Tkinter (Built-in GUI library)

📂 Project Structur

▶️ How to Run

1. Install Python 3 on your system.
2. Save the code as "workout_timer.py".
3. Open terminal or command prompt.
4. Run the following command:

python workout_timer.py

The application window will open and you can start the timer.

---

⚙️ Default Configuration

- Work Time: 30 seconds
- Rest Time: 10 seconds
- Total Rounds: 3

You can modify these values directly inside the script.


🔧 Customization Ideas

- Add Start / Pause / Reset buttons
- Add sound alerts when switching
- Allow user input for time and rounds
- Add dark mode styling
- Convert into an executable (.exe)

